x = [0.46, 0.47, 0.48, 0.49];
y = [0.4846555, 0.4937542, 0.5027498, 0.5116683];
x1 = 0.472;
y1 = interp1(x, y, x1, 'linear');
y2 = interp1(x, y, x1, 'spline');

disp(['线性插值得到的函数值为 ' num2str(y1, '%0.6f')]);
disp(['三次样条插值得到的函数值为 ' num2str(y2, '%0.6f')]);
